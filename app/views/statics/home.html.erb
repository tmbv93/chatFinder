
<% if logged_in? %>

    <!-- Dette vises kun om du er logget inn -->
    <%= render 'login_home' %>

<% else %>

    <!-- Dette vises kun om du IKKE er logget inn -->

    <div id="signInPortal">

      <div id="logoWrap" class="columns centered">
        <%= image_tag 'banner.png', id: 'bannerLogo' %>
      </div>

      <div id="infoWrap" class="columns large-6 medium-6">

        <div class="row">
          <div class="large-10 large-centered columns">
            <h1 class="theme-white-text">Se opp!</h1>
            <p id="introText" class="theme-white-text">
              Ikke gjem blikket i mobilen! Bli kjent med dine medreisende i stedet. Small bruker beacon-teknologi til å koble deg til andre mennesker som reiser på samme buss, trikk eller bane.

              <br><br>Bli kjent med ditt reisefølge idag.</p>
          </div>
        </div>

      </div>

      <div id="loginWrap" class="columns large-6 medium-6">
        <div class="row">
          <div class="large-9 columns">
            <div id="loginBox">
              <%= render 'sessions/form' %>
            </div>
            </div>
        </div>
      </div>

    </div>

<% end  %>
